akka {
  persistence {
    journal {
      plugin              = inmemory-journal
    }

    snapshot-store{
      plugin                     = inmemory-snapshot-store
    }
  }

  remote {
    artery {
      canonical.hostname = "127.0.0.1"
      canonical.port     = 0
    }
  }
}
